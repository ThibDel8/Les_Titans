{% set colors = {
    publicColor: '#b6882a',
    managerColor: '#c76620',
    greyLight: '#f5f5f5',
    grey: '#2a2a2a',
    greyDark: '#1c1c1c',
    black: '#0f0f0f',
    red: '#ef4444',
    green: '#22c55e',
} %}

<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block subject %}Les Titans | {% endblock %}</title>
    </head>
    <body style="font-family: Arial, sans-serif; background-color: {{ colors.black }}; margin: 0; padding: 0;">
        {# Bandeau supérieur #}
        {% include 'emails/_partials/_header.html.twig' %}

        {# Contenu principal #}
        <div style="
            max-width: 1200px;
            margin: auto;
            padding: 40px 20px;
            display: grid;
            grid-template-columns: 1fr;
        ">
            {% block body %}{% endblock %}
        </div>

        {# Footer #}
        {% include 'emails/_partials/_footer.html.twig' %}
    </body>
</html>
