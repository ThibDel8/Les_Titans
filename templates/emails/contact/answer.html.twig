{% extends 'emails/base.html.twig' %}

{% block subject %}{{ parent() }}Réponse au message de contact{% endblock %}

{% block body %}
    <h1 style="color: {{ colors.publicColor }};">Réponse au message de contact</h1>
    <div style="color: {{ colors.greyLight }}; margin-bottom: 20px;">
        <p>Bonjour,</p>
        <p>{{ message.answeredBy.firstname }}, {{ message.answeredBy.role|lower }} de l'association, a répondu à votre message.</p>
    </div>

    <section style="
        background-color: {{ colors.grey }};
        border-radius: 4px;
        padding: 25px;
        margin-bottom: 40px;
        border-left: 5px solid {{ colors.publicColor }};
    ">
        <h2 style="color: {{ colors.publicColor }}; margin-bottom: 40px;">Réponse</h2>
        <div style="color: {{ colors.greyLight }};">
            {{ message.answer }}
        </div><br>
        <h2 style="color: {{ colors.publicColor }}; margin-bottom: 40px;">Rappel de la demande</h2>
        <div style="color: {{ colors.greyLight }};">
            <strong>Sujet : </strong>{{ message.subject }}<br><br>
            <strong>Message : </strong><br>
            {{ message.body }}
        </div>
    </section>

    <div style="color: {{ colors.greyLight }};">
        <p>
            Pour poursuivre cet échange, vous pouvez contacter
            {{ message.answeredBy.firstname }} à l’adresse suivante :
            <a href="mailto:{{ message.answeredBy.email }}" style="color: {{ colors.publicColor }}; text-decoration: none;">{{ message.answeredBy.email }}</a>.
        </p>
        <p>Pour toute nouvelle demande, merci d’utiliser le formulaire de contact disponible sur notre site.</p><br>
        <p>Merci,</p>
        <p>Saint-Ouen Musculation | Les Titans</p>
    </div>
{% endblock %}
