{% if message.isUnread == 0 %}
<section class="gray-section">
{% else %}
<section class="manager-section">
{% endif %}
    <a class="manager" href="{{ path('admin_message_read', {id: message.id}) }}">
        <div class="member-list-names">
            <div>
                <p>{{ message.subject }}</p>
                <div class="membership-list-date">
                    <p>Envoyé par {{ message.email }}.</p>
                    <p>{{ message.createdAt|fr_date }}</p>
                </div>
            </div>
        </div>
    </a>
</section>
